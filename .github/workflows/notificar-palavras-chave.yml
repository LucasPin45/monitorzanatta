name: Monitor Palavras-chave

on:
  schedule:
    # ============================================================
    # HORÁRIOS OTIMIZADOS (Brasília → UTC)
    # Ajustados para evitar concorrência com notificar-tramitacoes
    # ============================================================
    
    # BOM DIA: 08:05 BRT = 11:05 UTC (10min depois do outro)
    - cron: '5 11 * * 1-5'
    
    # VARREDURAS: de 3 em 3h (menos frequente, horários diferentes)
    # 09:00 BRT = 12:00 UTC
    # 12:00 BRT = 15:00 UTC
    # 15:00 BRT = 18:00 UTC
    # 18:00 BRT = 21:00 UTC
    - cron: '0 12,15,18,21 * * 1-5'
    
    # RESUMO DO DIA: 20:45 BRT = 23:45 UTC (15min depois do outro)
    - cron: '45 23 * * 1-5'
  
  workflow_dispatch:
    inputs:
      modo:
        description: 'Modo de execução'
        required: true
        default: 'varredura'
        type: choice
        options:
          - varredura
          - bom_dia
          - resumo

# Permissões explícitas para fazer commits
permissions:
  contents: write

jobs:
  # ============================================================
  # JOB: BOM DIA (08:05 BRT)
  # ============================================================
  bom_dia:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    if: github.event.schedule == '5 11 * * 1-5' || (github.event_name == 'workflow_dispatch' && github.event.inputs.modo == 'bom_dia')
    
    steps:
      - name: Checkout do código
        uses: actions/checkout@v4
      
      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      
      - name: Instalar dependências
        run: pip install requests
      
      - name: Enviar Bom Dia
        env:
          # Telegram (pode usar tokens específicos ou os gerais)
          TELEGRAM_BOT_TOKEN_PALAVRAS: ${{ secrets.TELEGRAM_BOT_TOKEN_PALAVRAS }}
          TELEGRAM_CHAT_ID_PALAVRAS: ${{ secrets.TELEGRAM_CHAT_ID_PALAVRAS }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          # Email
          EMAIL_SMTP_SERVER: ${{ secrets.EMAIL_SMTP_SERVER }}
          EMAIL_SMTP_PORT: ${{ secrets.EMAIL_SMTP_PORT }}
          EMAIL_SENDER: ${{ secrets.EMAIL_SENDER }}
          EMAIL_PASSWORD: ${{ secrets.EMAIL_PASSWORD }}
          EMAIL_RECIPIENTS_PALAVRAS: ${{ secrets.EMAIL_RECIPIENTS_PALAVRAS }}
          EMAIL_RECIPIENTS: ${{ secrets.EMAIL_RECIPIENTS }}
          # Controle de canais
          NOTIFICAR_TELEGRAM: ${{ secrets.NOTIFICAR_TELEGRAM }}
          NOTIFICAR_EMAIL: ${{ secrets.NOTIFICAR_EMAIL }}
          # Modo
          MODO_EXECUCAO: bom_dia
        run: python notificar_palavras_chave.py
      
      - name: Salvar estado no repositório
        if: always()
        continue-on-error: true
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add -A *palavras_chave*.json 2>/dev/null || true
          git diff --staged --quiet || git commit -m "🔑 Atualiza estado do monitor de palavras-chave [skip ci]"
          git push || true

  # ============================================================
  # JOB: VARREDURA (de 3 em 3h)
  # ============================================================
  varredura:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    if: github.event.schedule == '0 12,15,18,21 * * 1-5' || (github.event_name == 'workflow_dispatch' && github.event.inputs.modo == 'varredura')
    
    steps:
      - name: Checkout do código
        uses: actions/checkout@v4
      
      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      
      - name: Instalar dependências
        run: pip install requests
      
      - name: Executar varredura
        env:
          # Telegram
          TELEGRAM_BOT_TOKEN_PALAVRAS: ${{ secrets.TELEGRAM_BOT_TOKEN_PALAVRAS }}
          TELEGRAM_CHAT_ID_PALAVRAS: ${{ secrets.TELEGRAM_CHAT_ID_PALAVRAS }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          # Email
          EMAIL_SMTP_SERVER: ${{ secrets.EMAIL_SMTP_SERVER }}
          EMAIL_SMTP_PORT: ${{ secrets.EMAIL_SMTP_PORT }}
          EMAIL_SENDER: ${{ secrets.EMAIL_SENDER }}
          EMAIL_PASSWORD: ${{ secrets.EMAIL_PASSWORD }}
          EMAIL_RECIPIENTS_PALAVRAS: ${{ secrets.EMAIL_RECIPIENTS_PALAVRAS }}
          EMAIL_RECIPIENTS: ${{ secrets.EMAIL_RECIPIENTS }}
          # Controle de canais
          NOTIFICAR_TELEGRAM: ${{ secrets.NOTIFICAR_TELEGRAM }}
          NOTIFICAR_EMAIL: ${{ secrets.NOTIFICAR_EMAIL }}
          # Modo
          MODO_EXECUCAO: varredura
        run: python notificar_palavras_chave.py
      
      - name: Salvar estado no repositório
        if: always()
        continue-on-error: true
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add -A *palavras_chave*.json 2>/dev/null || true
          git diff --staged --quiet || git commit -m "🔑 Atualiza estado do monitor de palavras-chave [skip ci]"
          git push || true

  # ============================================================
  # JOB: RESUMO DO DIA (20:45 BRT)
  # ============================================================
  resumo:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    if: github.event.schedule == '45 23 * * 1-5' || (github.event_name == 'workflow_dispatch' && github.event.inputs.modo == 'resumo')
    
    steps:
      - name: Checkout do código
        uses: actions/checkout@v4
      
      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      
      - name: Instalar dependências
        run: pip install requests
      
      - name: Enviar Resumo do Dia
        env:
          # Telegram
          TELEGRAM_BOT_TOKEN_PALAVRAS: ${{ secrets.TELEGRAM_BOT_TOKEN_PALAVRAS }}
          TELEGRAM_CHAT_ID_PALAVRAS: ${{ secrets.TELEGRAM_CHAT_ID_PALAVRAS }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          # Email
          EMAIL_SMTP_SERVER: ${{ secrets.EMAIL_SMTP_SERVER }}
          EMAIL_SMTP_PORT: ${{ secrets.EMAIL_SMTP_PORT }}
          EMAIL_SENDER: ${{ secrets.EMAIL_SENDER }}
          EMAIL_PASSWORD: ${{ secrets.EMAIL_PASSWORD }}
          EMAIL_RECIPIENTS_PALAVRAS: ${{ secrets.EMAIL_RECIPIENTS_PALAVRAS }}
          EMAIL_RECIPIENTS: ${{ secrets.EMAIL_RECIPIENTS }}
          # Controle de canais
          NOTIFICAR_TELEGRAM: ${{ secrets.NOTIFICAR_TELEGRAM }}
          NOTIFICAR_EMAIL: ${{ secrets.NOTIFICAR_EMAIL }}
          # Modo
          MODO_EXECUCAO: resumo
        run: python notificar_palavras_chave.py
      
      - name: Salvar estado no repositório
        if: always()
        continue-on-error: true
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add -A *palavras_chave*.json 2>/dev/null || true
          git diff --staged --quiet || git commit -m "🔑 Atualiza estado do monitor de palavras-chave [skip ci]"
          git push || true
