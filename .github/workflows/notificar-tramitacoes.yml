name: Notificar Tramitações

on:
  schedule:
    # ============================================================
    # HORÁRIOS (Brasília → UTC)
    # ============================================================
    # 
    # BOM DIA: 07:55 BRT = 10:55 UTC
    - cron: '55 10 * * 1-5'
    
    # VARREDURAS: de 2 em 2h
    # 08:00 BRT = 11:00 UTC
    # 10:00 BRT = 13:00 UTC
    # 12:00 BRT = 15:00 UTC
    # 14:00 BRT = 17:00 UTC
    # 16:00 BRT = 19:00 UTC
    # 18:00 BRT = 21:00 UTC
    # 20:00 BRT = 23:00 UTC
    - cron: '0 11,13,15,17,19,21,23 * * 1-5'
    
    # RESUMO DO DIA: 20:30 BRT = 23:30 UTC
    - cron: '30 23 * * 1-5'
  
  workflow_dispatch:
    inputs:
      modo:
        description: 'Modo de execução'
        required: true
        default: 'varredura'
        type: choice
        options:
          - varredura
          - bom_dia
          - resumo

# Permissões explícitas para fazer commits
permissions:
  contents: write

jobs:
  # ============================================================
  # JOB: BOM DIA (07:55 BRT)
  # ============================================================
  bom_dia:
    runs-on: ubuntu-latest
    if: github.event.schedule == '55 10 * * 1-5' || (github.event_name == 'workflow_dispatch' && github.event.inputs.modo == 'bom_dia')
    
    steps:
      - name: Checkout do código
        uses: actions/checkout@v4
      
      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Instalar dependências
        run: pip install requests
      
      - name: Enviar Bom Dia
        env:
          # Telegram
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          # Email
          EMAIL_SMTP_SERVER: ${{ secrets.EMAIL_SMTP_SERVER }}
          EMAIL_SMTP_PORT: ${{ secrets.EMAIL_SMTP_PORT }}
          EMAIL_SENDER: ${{ secrets.EMAIL_SENDER }}
          EMAIL_PASSWORD: ${{ secrets.EMAIL_PASSWORD }}
          EMAIL_RECIPIENTS: ${{ secrets.EMAIL_RECIPIENTS }}
          # Controle de canais (deixe vazio ou "false" para desabilitar)
          NOTIFICAR_TELEGRAM: ${{ secrets.NOTIFICAR_TELEGRAM }}
          NOTIFICAR_EMAIL: ${{ secrets.NOTIFICAR_EMAIL }}
          # Modo
          MODO_EXECUCAO: bom_dia
        run: python notificar_tramitacoes.py
      
      - name: Salvar estado no repositório
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add -A *.json 2>/dev/null || true
          git diff --staged --quiet || git commit -m "🤖 Atualiza estado do monitor [skip ci]"
          git push || true

  # ============================================================
  # JOB: VARREDURA (de 2 em 2h)
  # ============================================================
  varredura:
    runs-on: ubuntu-latest
    if: github.event.schedule == '0 11,13,15,17,19,21,23 * * 1-5' || (github.event_name == 'workflow_dispatch' && github.event.inputs.modo == 'varredura')
    
    steps:
      - name: Checkout do código
        uses: actions/checkout@v4
      
      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Instalar dependências
        run: pip install requests
      
      - name: Executar varredura
        env:
          # Telegram
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          # Email
          EMAIL_SMTP_SERVER: ${{ secrets.EMAIL_SMTP_SERVER }}
          EMAIL_SMTP_PORT: ${{ secrets.EMAIL_SMTP_PORT }}
          EMAIL_SENDER: ${{ secrets.EMAIL_SENDER }}
          EMAIL_PASSWORD: ${{ secrets.EMAIL_PASSWORD }}
          EMAIL_RECIPIENTS: ${{ secrets.EMAIL_RECIPIENTS }}
          # Controle de canais
          NOTIFICAR_TELEGRAM: ${{ secrets.NOTIFICAR_TELEGRAM }}
          NOTIFICAR_EMAIL: ${{ secrets.NOTIFICAR_EMAIL }}
          # Modo
          MODO_EXECUCAO: varredura
        run: python notificar_tramitacoes.py
      
      - name: Salvar estado no repositório
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add -A *.json 2>/dev/null || true
          git diff --staged --quiet || git commit -m "🤖 Atualiza estado do monitor [skip ci]"
          git push || true

  # ============================================================
  # JOB: RESUMO DO DIA (20:30 BRT)
  # ============================================================
  resumo:
    runs-on: ubuntu-latest
    if: github.event.schedule == '30 23 * * 1-5' || (github.event_name == 'workflow_dispatch' && github.event.inputs.modo == 'resumo')
    
    steps:
      - name: Checkout do código
        uses: actions/checkout@v4
      
      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Instalar dependências
        run: pip install requests
      
      - name: Enviar Resumo do Dia
        env:
          # Telegram
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          # Email
          EMAIL_SMTP_SERVER: ${{ secrets.EMAIL_SMTP_SERVER }}
          EMAIL_SMTP_PORT: ${{ secrets.EMAIL_SMTP_PORT }}
          EMAIL_SENDER: ${{ secrets.EMAIL_SENDER }}
          EMAIL_PASSWORD: ${{ secrets.EMAIL_PASSWORD }}
          EMAIL_RECIPIENTS: ${{ secrets.EMAIL_RECIPIENTS }}
          # Controle de canais
          NOTIFICAR_TELEGRAM: ${{ secrets.NOTIFICAR_TELEGRAM }}
          NOTIFICAR_EMAIL: ${{ secrets.NOTIFICAR_EMAIL }}
          # Modo
          MODO_EXECUCAO: resumo
        run: python notificar_tramitacoes.py
      
      - name: Salvar estado no repositório
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add -A *.json 2>/dev/null || true
          git diff --staged --quiet || git commit -m "🤖 Atualiza estado do monitor [skip ci]"
          git push || true
